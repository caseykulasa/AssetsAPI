get:
  tags:
    - ExternalAsset
  summary: List all assettypes available into the application for provided api-key.
  responses:
    '200':
      description: Success
      content:
        text/plain:
          schema:
            type: array
            items:
              type: string
        application/json:
          schema:
            type: array
            items:
              type: string
        text/json:
          schema:
            type: array
            items:
              type: string
    '500':
      description: Server Error
